{
	"schemaVersion": 1,
	"id": "chatlog",
	"version": "${mod_version}",
	"name": "${mod_name}",
	"description": "${mod_description}",
	"authors": [
		"${nolij}",
		"${embeddedt}"
	],
	"contact": {
		"website": "${repo_url}",
		"repo": "${repo_url}",
		"issues": "${repo_url}/issues"
	},
	"license": "OSL-3.0",
	"environment": "client",
	"entrypoints": {
		"client": [
			"dev.nolij.chatlog.ChatLog"
		],
		"modmenu": [
			"dev.nolij.chatlog.integration.ModMenuConfig"
		]
	},
	"accessWidener": "chatlog.accesswidener",
	"mixins": [
		"chatlog.mixins.json"
	],
	"depends": {
		"fabricloader": ">=${fabric_version}",
		"minecraft": ">=${minecraft_version}",
		"fabric": "*"
	}
}
